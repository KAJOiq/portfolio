---
import Section from "./Section.astro";
import type { CoursesProps } from "@types";

interface Props {
  courses: CoursesProps[];
}

const { courses } = Astro.props;
---

<Section text="Courses and Bootcamp" href="courses">
  {
    courses.map(({ institution, course, startDate, endDate, summary, sourceUrl }) => (
      <div class="mb-10">
        <h3 class="mb-1.5 font-serif text-2xl font-semibold text-white">
          {institution}
        </h3>
        <div class="flex flex-col items-start pb-5">
          <h4 class="mb-0.5 font-serif text-2xl font-medium text-primary">
            {course}
          </h4>
          <span class="pb-[2px] text-sm text-white/70">
            {startDate} — {endDate}
          </span>
          <div class="flex gap-5 pt-10 text-white">
          {sourceUrl && (
            <a
              href={sourceUrl}
              target="_blank"
              class="after:relative after:bottom-[-5px] after:content-[url(/external.svg)] hover:underline"
            >
              View Certificate
            </a>
          )}
          </div>
        </div>
        {Array.isArray(summary) ? (
          <ul class="list-none">
            {summary.map((log) => (
              <li class="relative mb-3 pl-8 text-base text-neutral before:absolute before:top-1 before:left-0 before:content-[url(/check.svg)]">
                {log}
              </li>
            ))}
          </ul>
        ) : (
          <p class="text-base text-neutral">{summary}</p>
        )}
      </div>
    ))
  }
</Section>
